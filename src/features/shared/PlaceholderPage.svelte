<script lang="ts">import Button from '../../design-system/components/Button.svelte'; import Card from '../../design-system/components/Card.svelte'; let { title, description = 'Esta funcionalidade ainda não possui conteúdo disponível.', onBack }: { title: string; description?: string; onBack?: () => void } = $props();</script>
<div class="page"><h1>{title}</h1><p>{description}</p><Card padding="lg"><div class="empty"><span>✦</span><h2>{title}</h2><p>{description}</p><Button variant="secondary" onclick={onBack}>Voltar ao início</Button></div></Card></div>
<style>.page{width:min(100%,var(--content-max));margin:0 auto;padding:var(--space-10)}h1{font-size:var(--text-3xl)}.page>p{margin:var(--space-2) 0 var(--space-8);color:var(--text-secondary)}.empty{display:grid;min-height:22rem;place-items:center;place-content:center;gap:var(--space-3);text-align:center}.empty>span{color:var(--color-brand-500);font-size:2rem}.empty p{max-width:28rem;color:var(--text-secondary);line-height:1.6;margin-bottom:var(--space-3)}@media(max-width:680px){.page{padding:var(--space-5)}}</style>
