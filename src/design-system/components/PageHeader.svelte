<script lang="ts">import type {Snippet} from 'svelte';let{eyebrow,title,description,actions}:{eyebrow:string;title:string;description:string;actions?:Snippet}=$props();</script>
<header><div><p>{eyebrow}</p><h1>{title}</h1><span>{description}</span></div>{#if actions}<div class="actions">{@render actions()}</div>{/if}</header>
<style>header{display:flex;align-items:flex-end;justify-content:space-between;gap:var(--space-6)}p{color:var(--color-brand-500);font-size:var(--text-xs);font-weight:800;letter-spacing:.08em;text-transform:uppercase}h1{margin-top:var(--space-2);font-size:clamp(var(--text-2xl),3vw,var(--text-3xl));line-height:var(--leading-tight);letter-spacing:-.04em}span{display:block;margin-top:var(--space-2);color:var(--text-secondary);line-height:1.55}.actions{display:flex;gap:var(--space-3)}@media(max-width:680px){header{align-items:flex-start;flex-direction:column}}</style>
